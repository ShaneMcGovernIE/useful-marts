-- Sharing metadata, read by tooling and the manager detail pane only.
return {
  summary = "Mart lists with info and wrap: BUY shows how many of each item you already have (live), SELL shows the per-item sell price, and both lists wrap at the ends.",
  author = "Shane McGovern",
  contact = "https://github.com/ShaneMcGovernIE/useful-marts",
  tags = { "ui", "quality-of-life", "beginner" },
  differences = {
    changed = {
      "BUY item-box rows gain a compact '×N' line under the item name while the native ¥ buy price stays on the right; it updates as you buy",
      "SELL item-box rows gain a compact ¥ sell price under the item name while the native xN quantity stays on the right",
      "BUY and SELL lists wrap: Up from the first row goes to the last, Down from the last goes to the first",
    },
    added = {},
    known = {
      "sell price is half the buy price, like the vanilla quantity box",
      "key items, HMs and unknown item ids show no price (they cannot be sold)",
      "the bag, the PC and all other lists are untouched",
      "the quantity box and 'I can pay you' confirmation are unchanged",
    },
  },
  credits = {},
  compat = { engine = ">=0.0.0-0 <0.3.0", modApi = 2 },
}
